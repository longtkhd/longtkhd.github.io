/*
 * FileValidator Messages
 *
 * This contains all the text for the FileValidator component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FileValidator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FileValidator component!',
  },
});
